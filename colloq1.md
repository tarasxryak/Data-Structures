# <b>COLLOQ. #1</b>
## <i>вопросики-вопросики, ПАСХАЛКА В КОНЦЕ</i>
### 🎯 БЛОК 1: Фундамент и простые алгоритмы

#### Оценка сложности алгоритма по времени и памяти (продолжение)

---
- [ ] 31. Что такое нотация "Омега-большое" (Ω) и какую границу роста функции она описывает?
---
<details>
<summary>Ответ</summary>

Нотация Ω (Омега-большое) описывает **асимптотическую нижнюю границу** роста функции. `f(n) = Ω(g(n))` означает, что функция `f(n)` растет *не медленнее*, чем `g(n)`. Это оценка для **лучшего случая** работы алгоритма.

</details>

---
- [ ] 32. Что такое нотация "Тета-большое" (Θ) и почему она считается самой точной асимптотической оценкой?
---
<details>
<summary>Ответ</summary>

Нотация Θ (Тета-большое) описывает **асимптотически точную границу**. `f(n) = Θ(g(n))` означает, что `f(n)` зажата между `c₁*g(n)` и `c₂*g(n)`, то есть растет с такой же скоростью, как и `g(n)`. Она является точной, потому что одновременно является и верхней (O), и нижней (Ω) оценкой.

</details>

---
- [ ] 33. Расположите следующие классы сложности в порядке возрастания: O(n!), O(n log n), O(n²), O(2ⁿ), O(log n), O(n), O(1).
---
<details>
<summary>Ответ</summary>

O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ) < O(n!)

</details>

---
- [ ] 34. Алгоритм А имеет сложность O(n log n), а алгоритм Б — O(n²). Означает ли это, что алгоритм А *всегда* будет работать быстрее алгоритма Б на любых входных данных?
---
<details>
<summary>Ответ</summary>

Нет, не всегда. Асимптотическая нотация описывает поведение функции при `n`, стремящемся к бесконечности. На **маленьких значениях `n`** алгоритм Б (O(n²)) может работать быстрее из-за меньших константных множителей или более простых операций. Например, `1000 * n * log(n)` будет больше `n²` при малых `n`.

</details>

---
- [ ] 35. Что такое амортизированный анализ сложности?
---
<details>
<summary>Ответ</summary>

Это метод анализа, который определяет среднюю стоимость операции в последовательности операций. Он используется, когда одна "дорогая" операция происходит редко, а ее стоимость "размазывается" по множеству предшествующих "дешевых" операций, давая в среднем хорошую оценку. Классический пример — расширение динамического массива.

</details>

#### Сортировка слиянием (Merge Sort)

---
- [ ] 36. Какой основной принцип лежит в основе сортировки слиянием?
---
<details>
<summary>Ответ</summary>

Принцип "Разделяй и властвуй" (Divide and Conquer). Массив рекурсивно делится пополам до тех пор, пока не останутся подмассивы из одного элемента, а затем эти отсортированные подмассивы сливаются обратно в один большой отсортированный массив.

</details>

---
- [ ] 37. Опишите шаги алгоритма `merge`, который является ключевой частью сортировки слиянием.
---
<details>
<summary>Ответ</summary>

Функция `merge` принимает два отсортированных подмассива. Она создает временный массив и с помощью двух указателей (по одному на каждый подмассив) итеративно сравнивает текущие элементы и копирует меньший из них во временный массив, после чего сдвигает соответствующий указатель. Процесс продолжается, пока один из подмассивов не закончится, после чего остаток другого подмассива копируется в конец.

</details>

---
- [ ] 38. Какова временная сложность сортировки слиянием в худшем, среднем и лучшем случаях?
---
<details>
<summary>Ответ</summary>

Во всех трех случаях временная сложность одинакова и составляет **Θ(n log n)**. Алгоритм всегда делит массив пополам и всегда сливает все части, независимо от исходных данных.

</details>

---
- [ ] 39. Какова пространственная сложность (сложность по памяти) классической реализации сортировки слиянием?
---
<details>
<summary>Ответ</summary>

**O(n)**, так как для процедуры слияния требуется дополнительный массив размером, равным размеру сливаемых частей.

</details>

---
- [ ] 40. Является ли сортировка слиянием устойчивой?
---
<details>
<summary>Ответ</summary>

Да, является. Устойчивость означает, что относительный порядок элементов с одинаковыми ключами сохраняется. В процедуре `merge`, если элементы равны, мы можем договориться всегда брать элемент из левого подмассива первым, что и обеспечивает устойчивость.

</details>

#### Сортировка вставками (Insertion Sort)

---
- [ ] 41. Опишите принцип работы сортировки вставками.
---
<details>
<summary>Ответ</summary>

Алгоритм делит массив на две части: отсортированную и неотсортированную. Он последовательно берет первый элемент из неотсортированной части и вставляет его на правильное место в отсортированной части, сдвигая элементы вправо. Это похоже на то, как человек сортирует игральные карты в руке.

</details>

---
- [ ] 42. Какова временная сложность сортировки вставками в лучшем и худшем случаях?
---
<details>
<summary>Ответ</summary>

- **Лучший случай (массив уже отсортирован):** **O(n)**. Каждый новый элемент просто сравнивается с последним в отсортированной части и остается на месте.
- **Худший случай (массив отсортирован в обратном порядке):** **O(n²)**. Каждый новый элемент приходится "протаскивать" через всю отсортированную часть до самого начала.

</details>

---
- [ ] 43. В какой ситуации сортировка вставками может быть эффективнее, чем Quick Sort?
---
<details>
<summary>Ответ</summary>

На **почти отсортированных массивах** и на **очень маленьких массивах**. Из-за своей адаптивности на почти отсортированных данных она работает почти за линейное время. На маленьких массивах (например, < 16 элементов) она выигрывает у рекурсивных алгоритмов за счет отсутствия накладных расходов на рекурсию.

</details>

#### Быстрая сортировка (Quick Sort)

---
- [ ] 44. В чем заключается основной принцип быстрой сортировки?
---
<details>
<summary>Ответ</summary>

Также "Разделяй и властвуй". Выбирается "опорный элемент" (pivot), и массив делится на две части: элементы меньше опорного и элементы больше опорного. Опорный элемент становится на свое финальное место. Затем этот процесс рекурсивно применяется к двум полученным подмассивам.

</details>

---
- [ ] 45. Что такое "опорный элемент" (pivot) и как его выбор влияет на производительность?
---
<details>
<summary>Ответ</summary>

Опорный элемент — это элемент, относительно которого происходит разделение массива. Идеальный выбор — медиана, которая разделит массив на две равные части. Неудачный выбор (например, минимальный или максимальный элемент) приводит к разделению на пустой и `n-1` массив, что ведет к худшему случаю `O(n²)`.

</details>

---
- [ ] 46. Опишите алгоритм разделения (partition) по схеме Ломуто.
---
<details>
<summary>Ответ</summary>

1. Опорный элемент выбирается (часто последний).
2. Указатель `i` устанавливается перед началом массива.
3. Указатель `j` проходит от начала до элемента перед опорным.
4. Если `A[j]` меньше или равен опорному, `i` сдвигается, и `A[i]` меняется местами с `A[j]`.
5. В конце опорный элемент меняется местами с `A[i+1]`. В итоге `A[i+1]` — финальная позиция опорного элемента.

</details>

---
- [ ] 47. Какова временная сложность быстрой сортировки в среднем и худшем случаях?
---
<details>
<summary>Ответ</summary>

- **Средний случай:** **Θ(n log n)**.
- **Худший случай:** **O(n²)**.

</details>

---
- [ ] 48. Как можно избежать худшего случая в быстрой сортировке?
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
Основная цель — избежать постоянного выбора плохого опорного элемента.
1.  **Случайный выбор опорного элемента (Randomized Pivot):** Вместо того чтобы брать всегда первый/последний элемент, выбирать случайный элемент из подмассива и делать его опорным. Это делает возникновение худшего случая (`O(n²)`) крайне маловероятным, так как он больше не зависит от структуры входных данных, а от случайности.
2.  **Выбор медианы из трех (Median-of-three):** Взять первый, средний и последний элементы подмассива, найти их медиану и использовать ее в качестве опорного элемента. Это почти всегда предотвращает худший случай на уже отсортированных или обратно отсортированных данных.
3.  **IntroSort:** Гибридный алгоритм (используется во многих стандартных библиотеках). Он начинает с Quick Sort, но если глубина рекурсии превышает определенный порог (например, `2 * log n`), это сигнализирует о приближении к худшему случаю. Тогда алгоритм переключается на **пирамидальную сортировку (Heapsort)**, которая гарантирует `O(n log n)`, а для маленьких подмассивов использует **сортировку вставками**.

</details>

---
- [ ] 49. Является ли быстрая сортировка устойчивой?
---
<details>
<summary>Ответ</summary>

Нет, классические реализации (например, по схеме Хоара или Ломуто) не являются устойчивыми. Во время обменов в процедуре `partition` относительный порядок равных элементов может легко нарушиться.

</details>

---
- [ ] 50. Какова пространственная сложность быстрой сортировки?
---
<details>
<summary>Ответ</summary>

**O(log n)** в среднем случае. Эта память требуется для стека рекурсивных вызовов. В худшем случае, когда рекурсия вырождается в цепочку, сложность по памяти может достигать **O(n)**.

</details>

#### Сортировка подсчетом (Counting Sort)

---
- [ ] 51. В чем основная идея сортировки подсчетом?
---
<details>
<summary>Ответ</summary>

Это не сравнительная сортировка. Ее идея в том, чтобы посчитать количество вхождений каждого элемента в исходном массиве и затем, на основе этих подсчетов, восстановить отсортированный массив.

</details>

---
- [ ] 52. Какова временная и пространственная сложность сортировки подсчетом?
---
<details>
<summary>Ответ</summary>

**Временная сложность:** **O(n + k)**.
**Пространственная сложность:** **O(k)**.
Здесь `n` — количество элементов, а `k` — диапазон значений (max - min + 1).

</details>

---
- [ ] 53. Какое главное ограничение есть у сортировки подсчетом?
---
<details>
<summary>Ответ</summary>

Она эффективна только тогда, когда диапазон значений `k` сопоставим с количеством элементов `n`. Если мы сортируем числа в диапазоне от 1 до 1 миллиарда, нам потребуется массив на миллиард элементов, что непрактично.

</details>

#### Цифровая сортировка (Radix Sort)

---
- [ ] 54. Что такое цифровая сортировка (Radix Sort) и какой другой алгоритм она использует как вспомогательный?
---
<details>
<summary>Ответ</summary>

Это не сравнительная сортировка, которая сортирует целые числа, обрабатывая их по разрядам (цифрам). В качестве вспомогательного алгоритма она использует любую **устойчивую** сортировку, чаще всего — **сортировку подсчетом**.

</details>

---
- [ ] 55. Объясните разницу между LSD (Least Significant Digit) и MSD (Most Significant Digit) Radix Sort.
---
<details>
<summary>Ответ</summary>

- **LSD (по младшему разряду):** Сортировка начинается с самого правого (младшего) разряда и движется влево к старшему. Этот метод проще в реализации и является наиболее распространенным.
- **MSD (по старшему разряду):** Сортировка начинается со старшего разряда. Она работает рекурсивно, группируя числа по старшему разряду, а затем сортируя каждую группу по следующему разряду.

</details>

---
- [ ] 56. Какова временная сложность цифровой сортировки?
---
<details>
<summary>Ответ</summary>

**O(d * (n + k))**, где `d` — количество разрядов в максимальном числе, `n` — количество элементов, `k` — количество возможных значений для одного разряда (например, 10 для десятичных чисел). Если `d` и `k` считать константами, сложность можно упростить до **O(n)**.

</details>

### 🎯 БЛОК 4: Структуры данных и их реализации

#### Стек (Stack)

---
- [ ] 57. Какому принципу следует структура данных Стек (Stack)?
---
<details>
<summary>Ответ</summary>

**LIFO (Last-In, First-Out)** — "Последним пришел, первым ушел". Элемент, который был добавлен в стек последним, будет извлечен первым.

</details>

---
- [ ] 58. Назовите три основные операции стека и их сложность.
---
<details>
<summary>Ответ</summary>

1.  `push` (добавить элемент): **O(1)**
2.  `pop` (извлечь верхний элемент): **O(1)**
3.  `peek` / `top` (посмотреть верхний элемент): **O(1)**

</details>

---
- [ ] 59. Приведите два примера использования стека в программировании.
---
<details>
<summary>Ответ</summary>

1.  **Отслеживание вызовов функций (call stack):** Когда функция вызывается, она помещается в стек; когда завершается — извлекается.
2.  **Проверка сбалансированности скобок в выражении:** `({[]})`

</details>

#### Очередь (Queue)

---
- [ ] 60. Какому принципу следует структура данных Очередь (Queue)?
---
<details>
<summary>Ответ</summary>

**FIFO (First-In, First-Out)** — "Первым пришел, первым ушел". Элемент, который был добавлен в очередь первым, будет извлечен первым.

</details>

---
- [ ] 61. Как реализовать очередь на массиве, чтобы избежать смещения всех элементов при извлечении?
---
<details>
<summary>Ответ</summary>

Используя **циклический (кольцевой) буфер**. Вместо физического смещения элементов используются два указателя: `head` (начало очереди) и `tail` (конец очереди), которые перемещаются по кругу внутри массива.

</details>

---
- [ ] 62. Назовите основные операции очереди и их сложность.
---
<details>
<summary>Ответ</summary>

1.  `enqueue` (добавить в конец): **O(1)**
2.  `dequeue` (извлечь из начала): **O(1)**
3.  `front` / `peek` (посмотреть первый элемент): **O(1)**

</details>

#### Списки (Linked Lists)

---
- [ ] 63. Что такое односвязный список (Singly Linked List)?
---
<details>
<summary>Ответ</summary>

Это линейная структура данных, состоящая из узлов (nodes), где каждый узел содержит данные и указатель (ссылку) на следующий узел в последовательности.

</details>

---
- [ ] 64. Какова сложность вставки/удаления элемента в начало односвязного списка?
---
<details>
<summary>Ответ</summary>

**O(1)**. Нужно лишь перенаправить указатель `head`.

</details>

---
- [ ] 65. Какова сложность доступа к i-тому элементу в односвязном списке?
---
<details>
<summary>Ответ</summary>

**O(n)**, так как для доступа к элементу нужно последовательно пройти `i` узлов от начала списка.

</details>

---
- [ ] 66. Что такое двусвязный список (Doubly Linked List) и чем он отличается от односвязного?
---
<details>
<summary>Ответ</summary>

В двусвязном списке каждый узел, помимо указателя на следующий элемент (`next`), также хранит указатель на **предыдущий** элемент (`prev`).

</details>

---
- [ ] 67. Какое преимущество дает двусвязный список при удалении заданного узла (не по значению, а по указателю на сам узел)?
---
<details>
<summary>Ответ</summary>

Удаление можно выполнить за **O(1)**. Имея указатель на узел, мы можем сразу получить доступ к его соседям (`node.prev` и `node.next`) и "связать" их между собой, исключив текущий узел. В односвязном списке для этого пришлось бы искать предыдущий узел за O(n).

</details>

---
- [ ] 68. Что такое циклический список (Circular Linked List)?
---
<details>
<summary>Ответ</summary>

Это список, в котором указатель `next` последнего узла указывает не на `null`, а на первый узел (`head`), замыкая список в кольцо.

</details>

#### Реализации на списках

---
- [ ] 69. Как реализовать стек на базе односвязного списка?
---
<details>
<summary>Ответ</summary>

Операции стека удобно отображаются на операции с началом списка:
- `push` — это вставка в начало списка (O(1)).
- `pop` — это удаление из начала списка (O(1)).
Вершиной стека будет `head` списка.

</details>

---
- [ ] 70. Почему для эффективной реализации очереди на односвязном списке нужны указатели на `head` и `tail`?
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
- Указатель на `head` нужен для быстрой операции извлечения (`dequeue`) за **O(1)**.
- Указатель на `tail` (хвост/конец) нужен для быстрой операции добавления (`enqueue`) за **O(1)**.
Если бы у нас был только указатель `head`, то для добавления элемента в конец очереди нам пришлось бы каждый раз проходить весь список от начала до конца, чтобы найти последний узел. Это сделало бы операцию `enqueue` очень медленной — **O(n)**.

</details>

---
- [ ] 71. Как можно реализовать очередь с помощью двух стеков?
---
<details>
<summary>Ответ</summary>

Используются два стека: `inbox` и `outbox`.
-   **`enqueue(value)`:** `push` в `inbox`. (O(1))
-   **`dequeue()`:** Если `outbox` пуст, все элементы перекладываются из `inbox` в `outbox`. `pop` из `outbox`. (Амортизированная сложность O(1)).
Перекладывание элементов "переворачивает" их порядок, эмулируя FIFO.

</details>

### 🎯 БЛОК 2: Пирамиды и приоритеты

#### Пирамида (Heap)

---
- [ ] 72. Что такое двоичная пирамида (Binary Heap)? Назовите два ее основных свойства.
---
<details>
<summary>Ответ</summary>

Это древовидная структура данных, которая удовлетворяет двум свойствам:
1.  **Свойство пирамиды (Heap Property):** Ключ в любом узле не меньше (для min-heap) или не больше (для max-heap) ключей в его дочерних узлах.
2.  **Свойство формы (Shape Property):** Это почти полное двоичное дерево; все уровни, кроме, возможно, последнего, полностью заполнены, а последний уровень заполняется слева направо.

</details>

---
- [ ] 73. Как удобно представлять двоичную пирамиду в виде массива? Как найти родителя и дочерние элементы для элемента с индексом `i` (в 0-индексации)?
---
<details>
<summary>Ответ</summary>

Пирамиду очень удобно хранить в обычном массиве.
Для элемента с индексом `i`:
- **Родитель:** `(i - 1) / 2`
- **Левый потомок:** `2 * i + 1`
- **Правый потомок:** `2 * i + 2`

</details>

---
- [ ] 74. Опишите операцию "просеивания вниз" (sift down / heapify down).
---
<details>
<summary>Ответ</summary>

Используется для восстановления свойства пирамиды, когда оно нарушено в корне поддерева. Узел сравнивается со своими потомками. Если он нарушает свойство пирамиды (например, в max-heap он меньше одного из потомков), он меняется местами с **наибольшим** из потомков. Процесс рекурсивно продолжается для узла на новой позиции, пока он не "встанет на свое место".

</details>

---
- [ ] 75. Опишите операцию "просеивания вверх" (sift up / heapify up).
---
<details>
<summary>Ответ</summary>

Используется при добавлении нового элемента в конец пирамиды. Новый элемент сравнивается со своим родителем. Если он нарушает свойство пирамиды (например, в max-heap он больше родителя), они меняются местами. Процесс продолжается, пока элемент не "всплывет" на свою правильную позицию.

</details>

---
- [ ] 76. Объясните, почему построение пирамиды (build_heap) из неупорядоченного массива имеет временную сложность **O(n)**.
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
Алгоритм `build_heap` вызывает `sift_down` для всех не-листовых узлов, начиная с последнего. Интуитивно кажется, что это `n/2 * log(n)`, но это не так.
-   Около `n/2` узлов (листья) не требуют просеивания.
-   Около `n/4` узлов над ними требуют просеивания на глубину не более 1.
-   Около `n/8` узлов над ними — на глубину не более 2.
...и так далее.
Суммарное количество операций выражается рядом, который сходится к линейной величине. Большинство узлов находятся внизу дерева и требуют очень малого числа операций `sift_down`, поэтому общая сложность **O(n)**.

</details>

#### Пирамидальная сортировка (Heapsort)

---
- [ ] 77. Как работает пирамидальная сортировка (Heapsort)? Опишите ее шаги.
---
<details>
<summary>Ответ</summary>

1.  **Построение пирамиды:** Из исходного массива строится max-heap. Это занимает `O(n)`.
2.  **Сортировка:** Цикл выполняется `n-1` раз:
    a. Корень пирамиды (максимальный элемент) меняется местами с последним элементом пирамиды.
    b. Размер пирамиды уменьшается на 1 (отсортированный элемент теперь в конце массива).
    c. Для нового корня вызывается `sift_down`, чтобы восстановить свойство пирамиды. Это занимает `O(log n)`.
Итоговая сложность: `O(n) + (n-1) * O(log n) = O(n log n)`.

</details>

---
- [ ] 78. Какова временная и пространственная сложность пирамидальной сортировки?
---
<details>
<summary>Ответ</summary>

- **Временная сложность:** **O(n log n)** во всех случаях (лучшем, среднем, худшем).
- **Пространственная сложность:** **O(1)**. Это in-place сортировка.

</details>

---
- [ ] 79. Является ли пирамидальная сортировка устойчивой?
---
<details>
<summary>Ответ</summary>

Нет, не является. Операции `sift_down` могут легко изменить относительный порядок равных элементов.

</details>

#### Очередь с приоритетом (Priority Queue)

---
- [ ] 80. Что такое очередь с приоритетом (Priority Queue)?
---
<details>
<summary>Ответ</summary>

Это абстрактный тип данных, похожий на очередь, но каждый элемент имеет "приоритет". Элементы извлекаются не в порядке их добавления, а в порядке приоритета (элемент с наивысшим приоритетом извлекается первым).

</details>

---
- [ ] 81. Какая структура данных лучше всего подходит для эффективной реализации очереди с приоритетом?
---
<details>
<summary>Ответ</summary>

**Двоичная пирамида (Binary Heap)**. Она позволяет выполнять основные операции (`insert` и `extract-max/min`) за `O(log n)`.

</details>

---
- [ ] 82. Как реализовать операции `increase_priority` и `decrease_priority` в очереди с приоритетом на базе пирамиды?
---
<details>
<summary>Ответ</summary>

1. Найти элемент в пирамиде (это `O(n)`, если не хранить доп. указатели).
2. Изменить его приоритет.
3. Если приоритет **увеличился** (в max-heap), вызвать для него `sift_up`.
4. Если приоритет **уменьшился** (в max-heap), вызвать для него `sift_down`.
Чтобы найти элемент быстрее, чем за `O(n)`, можно использовать хеш-таблицу для хранения позиций элементов.

</details>

### 🎯 БЛОК 3: Продвинутые темы

#### Динамическое программирование (ДП)

---
- [ ] 83. Назовите два ключевых свойства задач, которые можно эффективно решить с помощью ДП.
---
<details>
<summary>Ответ</summary>

1.  **Оптимальная подструктура (Optimal Substructure):** Оптимальное решение задачи содержит в себе оптимальные решения подзадач.
2.  **Перекрывающиеся подзадачи (Overlapping Subproblems):** В процессе решения одни и те же подзадачи возникают многократно. ДП решает их один раз и сохраняет результат.

</details>

---
- [ ] 84. В чем разница между "топ-даун" (мемоизация) и "боттом-ап" (табуляция) подходами в ДП?
---
<details>
<summary>Ответ</summary>

- **Топ-даун (мемоизация):** Это рекурсивный подход. Мы пишем рекурсивную функцию, которая решает задачу, но перед вычислением проверяем, не был ли результат уже сохранен в кеше (массиве или хеш-таблице). Если был — возвращаем его.
- **Боттом-ап (табуляция):** Это итеративный подход. Мы создаем таблицу (массив `dp`) и последовательно заполняем ее, начиная с самых маленьких подзадач и двигаясь к более крупным, пока не решим исходную задачу.

</details>

---
- [ ] 85. **Задача "Кузнечик":** Кузнечик стоит на 1-й ступеньке лестницы из `N`. Он может прыгать на `+1` или `+2` ступеньки. Сколько способов добраться до `N`-й ступеньки? Сформулируйте рекуррентное соотношение.
---
<details>
<summary>Ответ</summary>

Пусть `dp[i]` — количество способов добраться до `i`-й ступеньки.
- Базы: `dp[1] = 1`, `dp[2] = 2` (1+1 или 2).
- Переход: На `i`-ю ступеньку можно попасть либо с `i-1`, либо с `i-2`. Значит, `dp[i] = dp[i-1] + dp[i-2]`. Это числа Фибоначчи.

</details>

#### Динамическое программирование: Рюкзак (Knapsack)

---
- [ ] 86. Опишите постановку классической **задачи о рюкзаке 0/1 (0/1 Knapsack)**.
---
<details>
<summary>Ответ</summary>

Есть `N` предметов, каждый с весом `w[i]` и стоимостью `v[i]`. Есть рюкзак с максимальной вместимостью `W`. Нужно выбрать такой набор предметов, чтобы их суммарный вес не превышал `W`, а суммарная стоимость была максимальной. Каждый предмет можно либо взять (1), либо не взять (0).

</details>

---
- [ ] 87. Какими параметрами будет описываться состояние `dp[i][w]` в решении задачи о рюкзаке 0/1?
---
<details>
<summary>Ответ</summary>

`dp[i][w]` — это максимальная стоимость, которую можно набрать, рассмотрев первые `i` предметов и имея рюкзак вместимостью `w`.

</details>

---
- [ ] 88. Напишите формулу перехода (рекуррентное соотношение) для задачи о рюкзаке 0/1.
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
При рассмотрении `i`-го предмета у нас есть два варианта:
1.  **Не брать `i`-й предмет:** В этом случае максимальная стоимость будет такой же, как если бы мы рассматривали `i-1` предмет с той же вместимостью рюкзака `w`. То есть, `dp[i-1][w]`.
2.  **Взять `i`-й предмет:** Это возможно, только если его вес `w[i]` не превышает текущую вместимость `w`. Если мы его берем, он приносит стоимость `v[i]`, а оставшуюся вместимость рюкзака `w - w[i]` нужно максимально "заполнить" с помощью предыдущих `i-1` предметов. Итоговая стоимость: `v[i] + dp[i-1][w - w[i]]`.

Мы выбираем лучший из этих двух вариантов. Итоговая формула:
`dp[i][w] = max( dp[i-1][w], v[i] + dp[i-1][w - w[i]] )`

</details>

---
- [ ] 89. В чем отличие задачи о **неограниченном рюкзаке (Unbounded Knapsack)** от задачи 0/1, и как это меняет формулу перехода?
---
<details>
<summary>Ответ</summary>

В неограниченном рюкзаке каждый предмет можно брать любое количество раз.
Это меняет формулу перехода: при рассмотрении варианта "взять `i`-й предмет" мы можем снова использовать `i`-й предмет для заполнения оставшегося веса.
Новая формула: `dp[i][w] = max( dp[i-1][w], v[i] + **dp[i]**[w - w[i]] )`

</details>

#### Динамическое программирование: НВП (LIS)

---
- [ ] 90. Сформулируйте задачу о **наибольшей возрастающей подпоследовательности (НВП / LIS)**.
---
<details>
<summary>Ответ</summary>

Дан массив чисел. Необходимо найти самую длинную подпоследовательность (не обязательно идущих подряд элементов), элементы которой строго возрастают.

</details>

---
- [ ] 91. Опишите ДП-решение задачи о НВП со сложностью **O(n²)**.
---
<details>
<summary>Ответ</summary>

Пусть `dp[i]` — длина НВП, которая заканчивается на элементе `a[i]`. Чтобы вычислить `dp[i]`, мы ищем среди всех `j < i` такой, что `a[j] < a[i]`, и `dp[j]` при этом максимален. Тогда `dp[i] = max(dp[j]) + 1`. Если таких `j` нет, `dp[i] = 1`. Итоговый ответ — максимум среди всех `dp[i]`.

</details>

---
- [ ] 92. Как можно оптимизировать решение задачи о НВП до сложности **O(n log n)**?
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
Идея заключается в том, чтобы поддерживать вспомогательный массив `tails`, где `tails[k]` хранит наименьший возможный последний элемент для возрастающей подпоследовательности длины `k+1`. Этот массив всегда будет отсортирован.

**Алгоритм:**
1.  Итерируемся по каждому элементу `num` из входного массива.
2.  Для `num` ищем с помощью бинарного поиска (`lower_bound`) позицию в `tails`.
3.  - Если `num` больше всех элементов в `tails`, мы добавляем его в конец, тем самым увеличивая длину самой длинной найденной НВП.
    - Если `num` находится где-то в середине, мы заменяем им элемент на найденной позиции. Это не увеличивает длину НВП, но "улучшает" ее: мы нашли подпоследовательность такой же длины, но заканчивающуюся на меньшее число, что дает больше шансов на ее продление в будущем.

Итоговая длина НВП равна финальному размеру массива `tails`.

</details>

#### Динамическое программирование: Редакционное расстояние

---
- [ ] 93. В чем суть задачи о **редакционном расстоянии (расстоянии Левенштейна)**?
---
<details>
<summary>Ответ</summary>

Найти минимальное количество односимвольных операций (вставка, удаление, замена), необходимых для превращения одной строки в другую.

</details>

---
- [ ] 94. Какими параметрами описывается состояние ДП `dp[i][j]` для вычисления редакционного расстояния между строками S1 и S2?
---
<details>
<summary>Ответ</summary>

`dp[i][j]` — это редакционное расстояние между первыми `i` символами строки S1 и первыми `j` символами строки S2.

</details>

---
- [ ] 95. Напишите формулу перехода для вычисления редакционного расстояния.
---
<details>
<summary>Ответ</summary>

Если `S1[i] == S2[j]`, то стоимость замены 0, и `dp[i][j] = dp[i-1][j-1]`.
Если `S1[i] != S2[j]`, то стоимость замены 1. Тогда мы выбираем минимум из трех операций:
1.  **Замена:** `dp[i-1][j-1] + 1`
2.  **Удаление** (из S1): `dp[i-1][j] + 1`
3.  **Вставка** (в S1): `dp[i][j-1] + 1`

Итого: `dp[i][j] = min(dp[i-1][j-1], dp[i-1][j], dp[i][j-1]) + 1`

</details>

#### Динамическое программирование: Битовые маски

---
- [ ] 96. Для какого типа задач применяется **ДП по битовым маскам**?
---
<details>
<summary>Ответ</summary>

Для задач, в которых нужно перебирать подмножества элементов, и количество этих элементов очень мало (обычно N ≤ 20). Типичный пример — задача коммивояжера.

</details>

---
- [ ] 97. Что обычно кодируется битовой маской в таких задачах?
---
<details>
<summary>Ответ</summary>

**Подмножество посещенных/использованных элементов.** Если `i`-й бит в маске равен 1, это значит, что `i`-й элемент уже включен в текущее подмножество. Если 0 — то не включен.

</details>

---
- [ ] 98. Приведите пример состояния ДП для задачи коммивояжера с использованием битовых масок.
---
<details>
<summary>Ответ</summary>

`dp[mask][i]` — минимальная длина пути, который начинается в стартовой вершине, проходит через все вершины из подмножества, заданного маской `mask`, и заканчивается в вершине `i`.

</details>

#### Квикселект (Quickselect)

---
- [ ] 99. Какую задачу решает **алгоритм Quickselect**?
---
<details>
<summary>Ответ</summary>

Задачу **поиска k-й порядковой статистики** в неупорядоченном массиве (то есть, нахождения k-го по величине элемента).

</details>

---
- [ ] 100. Как Quickselect связан с быстрой сортировкой (Quicksort)?
---
<details>
<summary>Ответ</summary>

Он использует ту же самую процедуру разделения `partition`. После разделения массива относительно опорного элемента, мы знаем его финальную позицию. Сравнивая эту позицию с `k`, мы понимаем, в какой из двух половин (левой или правой) нужно продолжать поиск, а вторую половину можно полностью отбросить.

</details>

---
- [ ] 101. Какова средняя и худшая временная сложность Quickselect и почему?
---
<details>
<summary>Ответ</summary>

- **Средняя сложность: O(n).** В отличие от Quicksort, который делает два рекурсивных вызова, Quickselect делает только один. Сумма `n + n/2 + n/4 + ...` сходится к `2n`, что является `O(n)`.
- **Худшая сложность: O(n²).** Возникает по тем же причинам, что и в Quicksort — при постоянном неудачном выборе опорного элемента.

</details>

#### Мастер-теорема (Master Theorem)

---
- [ ] 102. Для какого типа рекуррентных соотношений применима **Мастер-теорема**?
---
<details>
<summary>Ответ</summary>

Для анализа рекуррентных соотношений вида **`T(n) = aT(n/b) + f(n)`**, которые часто возникают в алгоритмах типа "разделяй и властвуй".

</details>

---
- [ ] 103. Сформулируйте три основных случая Мастер-теоремы.
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
Сравнивается `f(n)` с `n^(log_b a)`.
1.  **Если `f(n)` растет полиномиально медленнее, чем `n^(log_b a)`:**
    Работа в листьях дерева рекурсии доминирует.
    **Решение: `T(n) = Θ(n^(log_b a))`**
2.  **Если `f(n)` растет с той же скоростью, что и `n^(log_b a)` (с точностью до логарифмического множителя):**
    Работа на каждом уровне дерева рекурсии примерно одинакова.
    **Решение: `T(n) = Θ(n^(log_b a) * log n)`**
3.  **Если `f(n)` растет полиномиально быстрее, чем `n^(log_b a)` (и выполняется условие регулярности):**
    Работа в корне дерева рекурсии доминирует.
    **Решение: `T(n) = Θ(f(n))`**

</details>

---
- [ ] 104. Примените Мастер-теорему для анализа `T(n) = 7T(n/2) + n²` (Умножение матриц по Штрассену).
---
<details>
<summary>Ответ</summary>

- `a=7`, `b=2`, `f(n)=n²`.
- Вычисляем `n^(log_b a) = n^(log₂ 7)`.
- `log₂ 7` ≈ 2.81.
- Сравниваем `f(n) = n²` и `n^2.81`.
- `n²` растет медленнее, чем `n^2.81`. Это **первый случай** теоремы.
- **Результат:** `T(n) = Θ(n^(log₂ 7))`.

</details>

---
- [ ] 105. В каком случае Мастер-теорема неприменима?
---
<details>
<summary>Ответ</summary>

Когда `f(n)` и `n^(log_b a)` несравнимы полиномиально. Например, для `T(n) = 2T(n/2) + n log n`, `n^(log_b a) = n¹`. `n log n` растет быстрее `n`, но не полиномиально быстрее (нет `ε > 0`). Это "зазор" между 2 и 3 случаями, для которого нужна расширенная версия теоремы.

</details>

#### Дек, Очередь с минимумом

---
- [ ] 106. Что такое **Дек (Deque - Double-ended queue)** и какие операции он поддерживает за O(1)?
---
<details>
<summary>Ответ</summary>

Это двусторонняя очередь. Он поддерживает за **O(1)** (амортизированно) операции добавления и удаления элементов как в начало, так и в конец: `push_front`, `pop_front`, `push_back`, `pop_back`.

</details>

---
- [ ] 107. Какую задачу решает структура данных **"Очередь с минимумом/максимумом"**?
---
<details>
<summary>Ответ</summary>

Она должна поддерживать стандартные операции очереди (`enqueue`, `dequeue`) и дополнительно операцию `get_minimum` (или `get_maximum`) — все за **O(1)**.

</details>

---
- [ ] 108. Как реализовать очередь с минимумом за O(1), используя Дек?
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение (Алгоритм "скользящего минимума"):**
Для хранения очереди с минимумом нам понадобится обычная очередь для самих элементов и **Дек** для хранения кандидатов на минимум.
- **`enqueue(value)`:**
  1. Добавляем `value` в обычную очередь.
  2. Смотрим на Дек: пока он не пуст и элемент в его хвосте (`back`) больше, чем `value`, удаляем этот элемент из хвоста.
  3. Добавляем `value` в хвост Дека.
- **`get_minimum()`:**
  Минимумом в текущей очереди всегда будет элемент в голове (`front`) Дека. Возвращаем его.
- **`dequeue()`:**
  1. Извлекаем элемент `value` из обычной очереди.
  2. Если `value` совпадает с элементом в голове Дека, то удаляем элемент из головы Дека.

Таким образом Дек всегда хранит элементы текущей очереди в возрастающем порядке, и в его голове всегда находится актуальный минимум. Все операции имеют **амортизированную сложность O(1)**.

</details>

#### Бинпоиск (Binary Search)

---
- [ ] 109. Каково основное свойство, необходимое для применения **бинарного поиска**?
---
<details>
<summary>Ответ</summary>

**Монотонность.** Для массива это означает, что он должен быть отсортирован. Для функции `f(x)` это означает, что она должна быть неубывающей или невозрастающей.

</details>

---
- [ ] 110. Что именно находит `lower_bound(begin, end, value)` в отсортированном контейнере?
---
<details>
<summary>Ответ</summary>

Указатель на **первый элемент, который НЕ МЕНЬШЕ, чем `value`** (`>= value`). Если все элементы меньше `value`, вернет `end`.

</details>

---
- [ ] 111. Чем `upper_bound(begin, end, value)` отличается от `lower_bound`?
---
<details>
<summary>Ответ</summary>

`upper_bound` возвращает указатель на **первый элемент, который СТРОГО БОЛЬШЕ, чем `value`** (`> value`).

</details>

---
- [ ] 112. Как, используя `lower_bound` и `upper_bound`, можно посчитать количество вхождений элемента `x` в массив за O(log n)?
---
<details>
<summary>Ответ</summary>

`count = upper_bound(x) - lower_bound(x)`. `lower_bound` найдет начало диапазона равных `x` элементов, а `upper_bound` — позицию сразу после конца этого диапазона. Разница указателей даст количество.

</details>

---
- [ ] 113. Объясните концепцию **"бинарного поиска по ответу"**.
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
Это техника для решения задач оптимизации (найти `min` или `max` возможное значение).
**Идея:** Вместо того чтобы искать сам ответ, мы ищем его бинарным поиском в пространстве возможных ответов. Для этого нам нужна `check(x)` функция, которая может ответить на вопрос: "Возможно ли достичь цели со значением параметра `x`?".
**Условие:** Эта `check` функция должна быть монотонной. Например, если `check(5)` истинно, то и `check(4)` тоже истинно (для задачи на `max`).

**Алгоритм:**
1. Определяем границы `[L, R]` для возможного ответа.
2. Пока `L < R`, берем `mid = (L+R)/2`.
3. Вызываем `check(mid)`. Если `true`, значит `mid` — потенциально хороший ответ, и мы пытаемся найти ответ еще лучше (например, сдвигаем `L = mid`). Если `false`, то `mid` слишком хорош, и надо искать ответ похуже (сдвигаем `R = mid-1`).

</details>

---
- [ ] 114. Приведите пример задачи, решаемой бинарным поиском по ответу.
---
<details>
<summary>Ответ</summary>

**Задача "Коровники" (или "Агрессивные коровы"):**
Есть `N` стойл на прямой, заданных координатами. Нужно разместить `K` коров так, чтобы минимальное расстояние между любыми двумя коровами было как можно больше.
- **Поиск по ответу:** Мы ищем бинарным поиском минимальное расстояние `d`.
- **`check(d)`:** "Можно ли расставить `K` коров так, чтобы расстояние между любыми двумя было не меньше `d`?". Эту проверку можно сделать жадно за `O(N)`.

</details>

---
- [ ] 115. Чем отличается **бинарный поиск по вещественным числам** от поиска по целым?
---
<details>
<summary>Ответ</summary>

Основное отличие в условии остановки. Для целых чисел цикл заканчивается, когда `left > right`. Для вещественных чисел указатели `left` и `right` могут никогда не совпасть. Поэтому остановка происходит либо после **фиксированного числа итераций** (например, 100, что дает высокую точность), либо когда разница `right - left` становится меньше заданной точности `epsilon`.

</details>

---
- [ ] 116. Сравните Quick Sort и Merge Sort по ключевым параметрам: временная сложность (худшая/средняя), пространственная сложность, устойчивость, "in-place".
---
<details>
<summary>Ответ</summary>

| Параметр | Quick Sort | Merge Sort |
| :--- | :--- | :--- |
| **Среднее время** | `Θ(n log n)` | `Θ(n log n)` |
| **Худшее время** | `O(n²)` | `O(n log n)` |
| **Пространство** | `O(log n)` (для стека) | `O(n)` (для доп. массива) |
| **Устойчивость** | Нет | Да |
| **In-place** | Да (почти) | Нет |

</details>

---
- [ ] 117. Сравните Heapsort и Quicksort. В какой ситуации один может быть предпочтительнее другого?
---
<details>
<summary>Ответ</summary>

Heapsort предпочтительнее, когда **критически важна гарантия времени `O(n log n)`** и/или **минимальное потребление памяти `O(1)`**. Например, во встраиваемых системах или приложениях реального времени, где худший случай `O(n²)` у Quicksort недопустим. Quicksort же на практике часто оказывается быстрее из-за лучшей локальности кеша и меньших константных множителей.

</details>

---
- [ ] 118. Сравните сортировку подсчетом и цифровую сортировку.
---
<details>
<summary>Ответ</summary>

- **Принцип:** Сортировка подсчетом считает количество вхождений каждого элемента. Цифровая сортировка использует подсчет (или другую устойчивую сортировку) для сортировки по разрядам.
- **Применение:** Подсчет хорош для целых чисел в **узком диапазоне**. Цифровая сортировка хороша для целых чисел в **широком диапазоне** (или строк), так как она зависит от количества разрядов, а не от величины.
- **Связь:** Цифровая сортировка — это, по сути, многократное применение сортировки подсчетом.

</details>

---
- [ ] 119. Объясните, почему для LSD Radix Sort критически важна **устойчивость** вспомогательного алгоритма сортировки.
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение:**
Устойчивость означает, что относительный порядок элементов с одинаковыми ключами сохраняется.
Когда мы сортируем числа по разрядам, начиная с младшего (LSD), то после сортировки по `i`-му разряду, числа с одинаковой цифрой в этом разряде должны сохранить тот порядок, который у них был после сортировки по `(i-1)`-му разряду.
**Пример:** Сортируем `[170, 045, 075]`.
1. **Сортируем по младшему разряду (0, 5, 5):** `[170, 045, 075]`. Так как `45` шло раньше `75`, они так и остаются.
2. **Сортируем по среднему разряду (7, 4, 7):** `[045, 170, 075]`. Порядок `170` и `075` (у обоих цифра 7) сохранился с прошлого шага.
3. **Сортируем по старшему разряду (0, 1, 0):** `[045, 075, 170]`. Порядок `045` и `075` сохранился.
Если бы сортировка была неустойчивой, на шаге 2 `075` могло бы оказаться перед `170`, и финальный результат был бы неверным.

</details>

---
- [ ] 120. Почему массив не является эффективной структурой данных для реализации очереди, если не использовать циклический буфер?
---
<details>
<summary>Ответ</summary>

Потому что операция `dequeue` (удаление из начала) потребует сдвига всех оставшихся `n-1` элементов на одну позицию влево, чтобы заполнить освободившееся место. Это приведет к тому, что сложность `dequeue` станет **O(n)** вместо желаемой O(1).

</details>

---
- [ ] 121. В каких случаях двусвязный список имеет явное преимущество перед односвязным?
---
<details>
<summary>Ответ</summary>

1.  **Удаление узла по указателю:** В двусвязном списке это O(1), в односвязном — O(n), так как нужно найти предыдущий узел.
2.  **Проход в обратном направлении:** В двусвязном это тривиально, в односвязном — невозможно без дополнительных структур.
3.  **Вставка до/после заданного узла:** Вставка "до" в односвязном списке требует поиска предыдущего узла.

</details>

---
- [ ] 122. Можно ли реализовать Дек на базе двусвязного списка? Если да, то какова будет сложность его операций?
---
<details>
<summary>Ответ</summary>

Да, можно, и это очень естественная реализация.
- `push_front`/`pop_front` — это вставка/удаление в `head` списка.
- `push_back`/`pop_back` — это вставка/удаление в `tail` списка.
Все эти операции для двусвязного списка выполняются за **O(1)**.

</details>

---
- [ ] 123. Объясните концепцию "дозорного" или "барьерного" элемента (sentinel node) в списках. Какие проблемы он решает?
---
<details>
<summary>Ответ</summary>

Это фиктивный узел (часто `head` или `tail`), который всегда присутствует в списке, даже если он пуст. Он упрощает код, избавляя от необходимости проверять крайние случаи:
- **Проверка на `null`:** При вставке или удалении не нужно отдельно обрабатывать случаи, когда список пуст, или когда операция происходит с первым/последним элементом.
- **Единообразие кода:** Код для вставки/удаления в середину списка работает и для его границ.

</details>

---
- [ ] 124. Как найти `k`-й элемент с конца в односвязном списке за один проход?
---
<details>
<summary>Ответ</summary>

Используется **метод двух указателей**.
1.  Создаем два указателя, `p1` и `p2`, оба указывают на `head`.
2.  Сдвигаем указатель `p2` на `k` позиций вперед.
3.  Теперь двигаем оба указателя (`p1` и `p2`) вперед с одинаковой скоростью, пока `p2` не достигнет конца списка.
4.  Когда `p2` достигнет конца, `p1` будет указывать на искомый `k`-й элемент с конца.

</details>

#### Алгоритмические задачи на списках

---
- [ ] 125. Как определить, есть ли в односвязном списке цикл, за время O(n) и с памятью O(1)?
---
<details>
<summary>Ответ</summary>

**Развернутое пояснение (Алгоритм Флойда "черепаха и заяц"):**
1.  Создаются два указателя: `slow` ("черепаха") и `fast` ("заяц"). Оба сначала указывают на `head`.
2.  На каждом шаге итерации `slow` сдвигается на один узел вперед (`slow = slow.next`), а `fast` — на два узла вперед (`fast = fast.next.next`).
3.  Если `fast` или `fast.next` становится `null`, значит, цикла нет, и мы дошли до конца списка.
4.  Если в какой-то момент `slow` и `fast` встретятся (будут указывать на один и тот же узел), значит, в списке **есть цикл**. "Заяц" догнал "черепаху", двигаясь по кругу.

</details>

---
- [ ] 126. Опишите итеративный способ развернуть односвязный список.
---
<details>
<summary>Ответ</summary>

Используются три указателя: `prev` (предыдущий), `current` (текущий) и `next_node` (следующий).
1.  Инициализируем: `prev = null`, `current = head`.
2.  В цикле, пока `current != null`:
    a. Сохраняем следующий узел: `next_node = current.next`.
    b. "Разворачиваем" указатель текущего узла: `current.next = prev`.
    c. Сдвигаем указатели на шаг вперед: `prev = current`, `current = next_node`.
3.  После выхода из цикла `prev` будет указывать на новую голову развернутого списка.

</details>

---
- [ ] 127. Какова сложность по памяти у рекурсивного способа разворота односвязного списка?
---
<details>
<summary>Ответ</summary>

**O(n)**. Эта память расходуется на стек рекурсивных вызовов. Глубина рекурсии будет равна длине списка `n`.

</details>

---
- [ ] 128. Как слить два отсортированных односвязных списка в один отсортированный список?
---
<details>
<summary>Ответ</summary>

Аналогично процедуре `merge` из сортировки слиянием.
1.  Создаем фиктивный узел `dummy_head`, чтобы упростить код.
2.  Используем указатель `current`, который будет "собирать" новый список.
3.  Пока оба списка (`list1`, `list2`) не пусты, сравниваем их головы, присоединяем к `current.next` меньший из узлов и сдвигаем указатель в соответствующем списке.
4.  Когда один из списков закончится, просто присоединяем остаток другого списка к концу.
Сложность по времени **O(n+m)**, по памяти **O(1)**.

</details>

#### Применение и детализация структур данных

---
- [ ] 129. Как удалить произвольный элемент (не корень) из двоичной пирамиды?
---
<details>
<summary>Ответ</summary>

1.  **Найти элемент:** Если позиции не кешируются, это может занять `O(n)`.
2.  **Заменить:** Поменять удаляемый элемент местами с **последним** элементом в пирамиде.
3.  **Удалить:** Удалить последний элемент (теперь на его месте стоит тот, что мы хотели удалить).
4.  **Восстановить свойство:** Для элемента, который мы переместили на место удаленного, нужно восстановить свойство пирамиды. Он может быть либо больше своего родителя, либо меньше своих детей. Поэтому мы вызываем для него сначала `sift_up`, а затем `sift_down`. Одна из этих операций фактически выполнится, другая — нет.
Сложность (без поиска): **O(log n)**.

</details>

---
- [ ] 130. Как найти k-й по величине элемент в массиве за O(n log k), используя min-heap (пирамиду)?
---
<details>
<summary>Ответ</summary>

1.  Создаем `min-heap` размером `k`.
2.  Добавляем в него первые `k` элементов из массива.
3.  Итерируемся по оставшимся `n-k` элементам массива:
    a. Если текущий элемент `x` больше, чем корень пирамиды (`heap.peek()`), то `x` точно входит в `k` наибольших элементов.
    b. Удаляем корень (`heap.extract_min()`) и добавляем `x` в пирамиду (`heap.insert(x)`).
4.  После прохода по всему массиву в корне пирамиды будет находиться ровно `k`-й по величине элемент.

</details>

---
- [ ] 131. Какую роль играет очередь с приоритетом (на базе min-heap) в алгоритме Дейкстры?
---
<details>
<summary>Ответ</summary>

Она позволяет на каждом шаге алгоритма **эффективно (за O(log V)) находить вершину с наименьшей текущей дистанцией от источника** среди всех еще не посещенных вершин. Без очереди с приоритетом этот шаг занимал бы O(V), что привело бы к общей сложности O(V²), а с ней — O(E log V).

</details>

---
- [ ] 132. Зачем в алгоритме сжатия Хаффмана используется очередь с приоритетом?
---
<details>
<summary>Ответ</summary>

Она используется для построения дерева кодирования. На каждом шаге алгоритм должен выбрать **два символа (или поддерева) с наименьшими частотами**, чтобы объединить их в новое поддерево. Очередь с приоритетом (min-heap) идеально подходит для того, чтобы быстро находить и извлекать эти два минимальных элемента.

</details>

---
- [ ] 133. Как в задаче о рюкзаке 0/1, после заполнения таблицы ДП, восстановить сам набор предметов, а не только их максимальную стоимость?
---
<details>
<summary>Ответ</summary>

Нужно пройти по таблице `dp` в обратном порядке, от `dp[N][W]`.
На шаге `dp[i][w]` мы анализируем, как было получено это значение:
- Если `dp[i][w] == dp[i-1][w]`, значит, `i`-й предмет **не брали**. Переходим к `dp[i-1][w]`.
- Если `dp[i][w] != dp[i-1][w]`, значит, `i`-й предмет **брали**. Добавляем его в ответ, и переходим к состоянию `dp[i-1][w - w[i]]`.
Процесс продолжается, пока `i > 0`.

</details>

---
- [ ] 134. Какова пространственная сложность базового решения задачи о рюкзаке и как ее можно оптимизировать?
---
<details>
<summary>Ответ</summary>

- **Базовая сложность:** **O(N * W)**, так как нужна таблица `dp[N][W]`.
- **Оптимизация:** Можно заметить, что для вычисления `i`-й строки таблицы нам нужна только `(i-1)`-я. Это позволяет оптимизировать пространство до **O(W)**, используя всего два массива (текущий и предыдущий) или даже один, если итерироваться по весу в правильном порядке (с конца к началу).

</details>

---
- [ ] 135. Сформулируйте задачу о **наибольшей общей подпоследовательности (НОП / LCS)**.
---
<details>
<summary>Ответ</summary>

Даны две последовательности (например, строки). Нужно найти самую длинную подпоследовательность, которая присутствует в обеих исходных последовательностях.
Пример: для "AGGTAB" и "GXTXAYB", НОП — "GTAB".

</details>

---
- [ ] 136. Опишите состояние и переход для ДП-решения задачи о НОП.
---
<details>
<summary>Ответ</summary>

- **Состояние:** `dp[i][j]` — длина НОП для первых `i` символов первой строки и первых `j` символов второй строки.
- **Переход:**
  - Если `S1[i] == S2[j]`, то мы нашли общий символ и можем удлинить НОП для `S1[i-1]` и `S2[j-1]`: `dp[i][j] = 1 + dp[i-1][j-1]`.
  - Если `S1[i] != S2[j]`, то общий символ не найден, и мы берем лучшее из того, что было до этого: `dp[i][j] = max(dp[i-1][j], dp[i][j-1])`.

</details>

---
- [ ] 137. Можно ли применить бинарный поиск для нахождения элемента в отсортированном массиве, который был циклически сдвинут (rotated sorted array)?
---
<details>
<summary>Ответ</summary>

Да. Модифицированный бинарный поиск. На каждом шаге, после выбора `mid`, мы сначала определяем, какая из половин (`left..mid` или `mid..right`) является "нормальной" (неразрывной). Затем, проверяя, попадает ли искомый элемент в диапазон этой нормальной половины, мы решаем, в какой из половин продолжать поиск.

</details>

#### Общие, сравнительные и прикладные вопросы

---
- [ ] 138. Вам нужно хранить набор данных и часто выполнять операцию "найти и удалить медиану". Какую комбинацию структур данных вы бы использовали для этого?
---
<details>
<summary>Ответ</summary>

**Две пирамиды (кучи):**
- `max-heap` для хранения меньшей половины чисел.
- `min-heap` для хранения большей половины чисел.
Медианой всегда будет либо корень `max-heap`, либо корень `min-heap` (или их среднее). Размеры пирамид поддерживаются одинаковыми (разница не более 1). Вставка `O(log n)`, удаление медианы `O(log n)`.

</details>

---
- [ ] 139. Какая структура данных идеально подходит для реализации функции "Undo" (Отмена) в текстовом редакторе?
---
<details>
<summary>Ответ</summary>

**Стек**. Каждое действие пользователя (или состояние документа) помещается в стек. Операция "Undo" просто извлекает (`pop`) последнее действие из стека и откатывает изменения.

</details>

---
- [ ] 140. В чем ключевое отличие между **жадными алгоритмами** и **динамическим программированием**?
---
<details>
<summary>Ответ</summary>

- **Жадный алгоритм** на каждом шаге делает локально оптимальный выбор, не пересматривая его в будущем. Он не рассматривает все возможные варианты.
- **Динамическое программирование** находит решение, исследуя все возможные варианты (через решение всех подзадач) и выбирая из них глобально оптимальный.
Жадный алгоритм проще и быстрее, но применим к гораздо меньшему кругу задач.

</details>

---
- [ ] 141. У вас есть огромный файл (например, 100 Гб), который не помещается в оперативную память (RAM). Какой алгоритм сортировки вы выберете для его сортировки?
---
<details>
<summary>Ответ</summary>

**Внешняя сортировка слиянием (External Merge Sort)**.
1. Файл читается по частям (чанками), которые помещаются в RAM.
2. Каждый чанк сортируется (например, с помощью Quicksort) и сохраняется во временный файл.
3. Затем эти отсортированные временные файлы сливаются в один большой отсортированный файл с помощью k-путевого слияния.

</details>

---
- [ ] 142. Зачем может понадобиться **устойчивая сортировка**? Приведите практический пример.
---
<details>
<summary>Ответ</summary>

Она нужна, когда нужно сохранить исходный порядок элементов, равных по ключу сортировки.
**Пример:** У вас есть таблица студентов, уже отсортированная по алфавиту. Вы хотите отсортировать ее по номеру курса, но так, чтобы внутри каждого курса студенты по-прежнему шли по алфавиту. Если использовать устойчивую сортировку по курсу, этот исходный алфавитный порядок сохранится.

</details>

---
- [ ] 143. В чем заключается компромисс "время-память" (time-space tradeoff)? Приведите пример.
---
<details>
<summary>Ответ</summary>

Это принцип, согласно которому можно ускорить выполнение алгоритма за счет использования большего объема памяти, или наоборот, уменьшить потребление памяти за счет увеличения времени выполнения.
**Пример:** Сортировка подсчетом. Она очень быстрая (O(n+k)), но требует много памяти (O(k)) для массива счетчиков. Heapsort медленнее (O(n log n)), но не требует дополнительной памяти (O(1)).

</details>

---
- [ ] 144. Что такое "онлайн" алгоритм? Какой из изученных алгоритмов сортировки является таковым?
---
<details>
<summary>Ответ</summary>

Онлайн-алгоритм может обрабатывать входные данные по частям, не требуя их полного наличия с самого начала.
**Сортировка вставками (Insertion Sort)** является онлайн-алгоритмом. Она может начать сортировать данные по мере их поступления, поддерживая отсортированную часть в актуальном состоянии.

</details>

---
- [ ] 145. Почему `build_heap`, реализованный через последовательные `insert` (и `sift_up`), имеет сложность O(n log n), а не O(n)?
---
<details>
<summary>Ответ</summary>

Потому что при последовательных вставках каждый новый элемент может потребовать `sift_up` на всю высоту дерева. Особенно "дорогими" становятся последние `n/2` вставок, каждая из которых может стоить `O(log n)`. В отличие от стандартного `build_heap`, где большинство операций `sift_down` выполняются у "дна" дерева на малую глубину, здесь большинство операций `sift_up` выполняются "с самого дна" на большую высоту.

</details>

---
- [ ] 146. Реализуйте приоритетную очередь на базе **несортированного массива**. Какова сложность операций `insert` и `extract_max`?
---
<details>
<summary>Ответ</summary>

- `insert(value)`: Просто добавляем элемент в конец массива. **O(1)**.
- `extract_max()`: Нужно найти максимальный элемент во всем массиве (O(n)), а затем удалить его (например, поменяв с последним и уменьшив размер) (O(1)). Итого: **O(n)**.

</details>

---
- [ ] 147. Реализуйте приоритетную очередь на базе **сортированного массива**. Какова сложность операций `insert` и `extract_max`?
---
<details>
<summary>Ответ</summary>

- `insert(value)`: Найти место для элемента (O(log n) бинпоиском) и вставить его со сдвигом остальных (O(n)). Итого: **O(n)**.
- `extract_max()`: Максимальный элемент находится в конце массива. Просто извлекаем его. **O(1)**.

</details>

---
- [ ] 148. Сравнивая реализации из вопросов 146-147 с реализацией на пирамиде, почему пирамида считается лучшим выбором?
---
<details>
<summary>Ответ</summary>

Пирамида предоставляет **сбалансированный компромисс**. В то время как реализации на массивах делают одну операцию очень быстрой (O(1)), а другую — очень медленной (O(n)), пирамида делает обе ключевые операции (`insert` и `extract_max`) достаточно быстрыми — **O(log n)**.

</details>

---
- 149. Что такое "хвостовая рекурсия" и почему она важна для некоторых рекурсивных алгоритмов?
---
<details>
<summary>Ответ</summary>

Хвостовая рекурсия — это когда рекурсивный вызов является **самым последним действием** в функции. Современные компиляторы могут оптимизировать такие вызовы (Tail Call Optimization), заменяя их на итерацию (цикл). Это позволяет избежать роста стека вызовов и ошибки `Stack Overflow` для рекурсий большой глубины.

</details>

---
- [ ] 150. Вы хотите отсортировать массив, состоящий из чисел 0, 1 и 2 (например, `[0, 1, 2, 2, 1, 0, 0, 2]`). Какой алгоритм будет наиболее эффективным?
---
<details>
<summary>Ответ</summary>

**Сортировка подсчетом**. Здесь диапазон `k` всего 3, что делает ее идеальным кандидатом со сложностью O(n). Также подойдет **Голландский национальный флаг (Dutch National Flag problem)**, который решает задачу за один проход с тремя указателями и сложностью O(n) по времени и O(1) по памяти.

</details>

---
- [ ] 151. В чем заключается основная идея удаления элемента из пирамиды?
---
<details>
<summary>Ответ</summary>

Основная идея заключается в том, чтобы **избежать "дыры"** в структуре дерева. Мы заменяем удаляемый узел последним элементом пирамиды (чтобы сохранить свойство формы), а затем восстанавливаем свойство пирамиды для перемещенного элемента с помощью `sift_up` или `sift_down`.

</details>

---
- [ ] 152. Зачем в бинарном поиске по ответу нужна **монотонность** `check`-функции?
---
<details>
<summary>Ответ</summary>

Монотонность гарантирует, что если `check(x)` истинна, то для всех "худших" значений она тоже будет истинна, а если ложна — то для всех "лучших" значений она тоже будет ложна. Это свойство делит все пространство ответов на две четкие части (`true` и `false`), что и позволяет отбрасывать половину на каждом шаге бинарного поиска.

</details>

---
- [ ] 153. Объясните, почему `pop_back` у `std::vector` (динамического массива) имеет амортизированную сложность O(1), а не всегда O(1).
---
<details>
<summary>Ответ</summary>

Вопрос с подвохом. Операция `pop_back` у `std::vector` **всегда** имеет сложность O(1), так как она просто уменьшает счетчик размера и не затрагивает аллоцированную память. Амортизированная сложность O(1) относится к операции `push_back`, которая иногда вызывает дорогостоящую реалокацию массива.

</details>

---
- [ ] 154. Какой алгоритм выбрать для сортировки 1000 элементов, если скорость не критична, а простота реализации — главный приоритет?
---
<details>
<summary>Ответ</summary>

**Сортировка вставками (Insertion Sort)** или **сортировка пузырьком (Bubble Sort)**. Они реализуются в несколько строк кода и, хотя имеют сложность O(n²), на 1000 элементов отработают практически мгновенно на любом современном компьютере.

</details>

---
- [ ] 155. Итоговый выбор: вам нужно отсортировать массив из 1 миллиона 32-битных целых чисел в памяти. Какой алгоритм вы выберете и почему?
---
<details>
<summary>Ответ</summary>

**Introsort** (гибрид Quicksort, Heapsort и Insertion Sort), который используется в большинстве стандартных библиотек (`std::sort` в C++).
**Обоснование:**
1.  **Средний случай:** Он работает как Quicksort, который на практике очень быстр (O(n log n)).
2.  **Защита от худшего случая:** Он отслеживает глубину рекурсии и переключается на Heapsort, если есть риск скатиться в O(n²), гарантируя O(n log n).
3.  **Эффективность на малых подмассивах:** Он переключается на Insertion Sort для маленьких подмассивов, что быстрее рекурсивных вызовов.
Это самый сбалансированный и надежный выбор для общего случая.

</details>

<img src="fat_timurka.jpeg" align="center">